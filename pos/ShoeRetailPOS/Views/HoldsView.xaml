<UserControl x:Class="ShoeRetailPOS.Views.HoldsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#121212">

    <!-- ================= RESOURCES ================= -->
    <UserControl.Resources>
        <Color x:Key="PrimaryRedColor">#E53935</Color>
        <Color x:Key="DarkRedColor">#C62828</Color>

        <SolidColorBrush x:Key="PrimaryRed" Color="{StaticResource PrimaryRedColor}"/>
        <SolidColorBrush x:Key="DarkRed" Color="{StaticResource DarkRedColor}"/>

        <!-- Hold Card -->
        <Style x:Key="HoldCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Action Button -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background"
                                        Value="{StaticResource DarkRed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- ================= LAYOUT ================= -->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ================= HEADER ================= -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Held Sales"
                       Foreground="White"
                       FontSize="26"
                       FontWeight="Bold"/>
        </StackPanel>

        <!-- ================= CONTENT ================= -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- ===== LEFT: HELD SALES LIST ===== -->
            <ScrollViewer Grid.Column="0">
                <ListBox x:Name="HoldsList"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="White"
                         HorizontalContentAlignment="Stretch">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource HoldCardStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel>
                                        <TextBlock Text="{Binding HoldName}"
                                                   FontSize="15"
                                                   FontWeight="Bold"/>

                                        <TextBlock Text="{Binding DateTime}"
                                                   Foreground="#AAAAAA"
                                                   FontSize="12"/>

                                        <TextBlock Text="{Binding ItemCount, StringFormat={}{0} items}"
                                                   Foreground="#CCCCCC"
                                                   FontSize="12"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Total, StringFormat=Rs. {0:N2}}"
                                               Foreground="{StaticResource PrimaryRed}"
                                               FontWeight="Bold"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                </ListBox>
            </ScrollViewer>

            <!-- ===== RIGHT: ACTIONS ===== -->
            <StackPanel Grid.Column="1"
                        Margin="20,0,0,0"
                        VerticalAlignment="Top">

                <Button Content="Resume Sale"
                        Background="{StaticResource PrimaryRed}"
                        Style="{StaticResource ActionButton}"
                        Margin="0,0,0,12"/>

                <Button Content="Delete Hold"
                        Background="#444444"
                        Style="{StaticResource ActionButton}"
                        Margin="0,0,0,12"/>

                <Button Content="Back to Dashboard"
                        Background="#333333"
                        Style="{StaticResource ActionButton}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
