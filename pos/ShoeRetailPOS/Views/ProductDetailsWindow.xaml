<Window x:Class="ShoeRetailPOS.Views.ProductDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Product Details" Height="620" Width="1026"
        WindowStartupLocation="CenterOwner"
        Background= "Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize" WindowStyle="None">
    <Window.Resources>
        <!-- Colors (matching dashboard) -->
        <Color x:Key="PrimaryRedColor">#E53935</Color>
        <Color x:Key="DarkRedColor">#C62828</Color>
        <Color x:Key="DarkBlackColor">#121212</Color>
        <Color x:Key="LightBlackColor">#212121</Color>
        <Color x:Key="SoftBlackColor">#424242</Color>
        <Color x:Key="LightGrayColor">#F5F5F5</Color>
        <Color x:Key="BorderGrayColor">#E0E0E0</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="PrimaryRed" Color="{StaticResource PrimaryRedColor}"/>
        <SolidColorBrush x:Key="DarkRed" Color="{StaticResource DarkRedColor}"/>
        <SolidColorBrush x:Key="DarkBlack" Color="{StaticResource DarkBlackColor}"/>
        <SolidColorBrush x:Key="LightBlack" Color="{StaticResource LightBlackColor}"/>
        <SolidColorBrush x:Key="SoftBlack" Color="{StaticResource SoftBlackColor}"/>
        <SolidColorBrush x:Key="LightGray" Color="{StaticResource LightGrayColor}"/>
        <SolidColorBrush x:Key="BorderGray" Color="{StaticResource BorderGrayColor}"/>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryRed}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="16,12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource DarkRed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Quantity Button Style -->
        <Style x:Key="QuantityButton" TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="16" />
            <!-- CORRECTED LINE -->
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="18">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryRed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Name="DropDownBorder" Background="{TemplateBinding Background}" 
                                    CornerRadius="6"
                                    BorderBrush="#333333"
                                    BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"
                                          Content="{TemplateBinding SelectionBoxItem}"/>

                                    <ToggleButton Grid.Column="1" 
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Width="30"
                                                  Focusable="False"
                                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <TextBlock Text="&#xE70D;" FontFamily="Segoe MDL2 Assets" Foreground="White"/>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" PlacementTarget="{Binding ElementName=DropDownBorder}">
                                <Border Background="#252525" CornerRadius="6" BorderBrush="#333333" BorderThickness="1" MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderBrush="#333333"
                                BorderThickness="1">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#1E1E1E" CornerRadius="12" Margin="15">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.4" ShadowDepth="5" BlurRadius="15"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header with Close Button -->
            <Grid Grid.Row="0" Margin="20,20,20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Product Details" 
                           Foreground="White" FontSize="22" FontWeight="SemiBold"/>

                <Button Grid.Column="1" Background="Transparent" BorderThickness="0" 
                        Width="32" Height="32" Click="CloseButton_Click">
                    <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" 
                               Foreground="White" FontSize="16"/>
                </Button>
            </Grid>

            <!-- Main Content (Two Columns) -->
            <Grid Grid.Row="1" Margin="20,20,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT: Product Image -->
                <Border Grid.Column="0" CornerRadius="10" Margin="0,0,20,0" Background="#252525">
                    <Image x:Name="ProductImage" 
                           Stretch="UniformToFill" 
                           Margin="10"/>
                </Border>

                <!-- RIGHT: Product Details -->
                <StackPanel Grid.Column="1" VerticalAlignment="Top">
                    <!-- Product Name and Price -->
                    <TextBlock x:Name="ProductName" 
                               Foreground="White" FontWeight="SemiBold" FontSize="24" 
                               TextWrapping="Wrap" Margin="0,0,0,5"/>

                    <TextBlock x:Name="ProductPrice" 
                               Foreground="{StaticResource PrimaryRed}" FontSize="28" 
                               FontWeight="Bold" Margin="0,0,0,20"/>

                    <!-- Product Description -->
                    <Border Background="#252525" CornerRadius="8" Padding="15" Margin="0,0,0,25">
                        <TextBlock x:Name="ProductDescription" 
                                   Foreground="#CCCCCC" FontSize="14" 
                                   TextWrapping="Wrap"/>
                    </Border>

                    <!-- Size Selection -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Select Size" 
                                   Foreground="White" FontWeight="Medium" 
                                   FontSize="14" Margin="0,0,0,8"/>

                        <ComboBox
                                ItemsSource="{Binding Sizes}"
                                DisplayMemberPath="SizeValue"
                                SelectedValuePath="SizeValue"
                                SelectedValue="{Binding SelectedSizeTemp, Mode=TwoWay}"
                                Style="{StaticResource ModernComboBox}"
                                Height="45"/>



                    </StackPanel>

                    <!-- Quantity Selection -->
                    <StackPanel Margin="0,0,0,30">
                        <TextBlock Text="Quantity" 
                                   Foreground="White" FontWeight="Medium" 
                                   FontSize="14" Margin="0,0,0,8"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0" Content="-" 
                                    Style="{StaticResource QuantityButton}"
                                    Click="DecreaseQuantity_Click"/>

                            <TextBox Grid.Column="1" x:Name="QuantityBox" 
                                     Text="1"
                                     Style="{StaticResource ModernTextBox}" 
                                     Margin="10,0"/>

                            <Button Grid.Column="2" Content="+" 
                                    Style="{StaticResource QuantityButton}"
                                    Click="IncreaseQuantity_Click"/>
                        </Grid>
                    </StackPanel>

                    <!-- Add to Cart Button -->
                    <Button Content="Add to Cart" 
                            Style="{StaticResource ModernButton}" 
                            Height="50" FontSize="16"
                            Click="AddToCartButton_Click">
                        <Button.Effect>
                            <DropShadowEffect Color="{StaticResource PrimaryRedColor}" 
                                              Opacity="0.3" ShadowDepth="3" BlurRadius="8"/>
                        </Button.Effect>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>